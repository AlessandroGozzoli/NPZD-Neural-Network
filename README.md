# NPZD-Neural-Network
A feedforward neural network trained to emulate a marine NPZD biogeochemical model. Ground-truth data is generated by Monte Carlo integration of the NPZD ODE system. The trained MLP is evaluated via autoregressive rollout over an annual cycle, testing whether it can reproduce spring phytoplankton bloom without access to the underlying equations.
